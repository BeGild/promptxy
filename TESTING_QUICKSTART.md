# PromptXY 测试快速开始指南

## 🚀 快速开始

### 1. 安装测试依赖

```bash
cd frontend
npm install
```

### 2. 验证测试设置

```bash
node tests/validate-tests.mjs
```

### 3. 运行所有测试

```bash
npm run test:run
```

## 📁 测试文件位置

```
frontend/tests/unit/
├── utils.test.ts    # 工具函数 (69个测试)
├── api.test.ts      # API客户端 (35个测试)
├── hooks.test.ts    # React Hooks (29个测试)
└── store.test.ts    # 状态管理 (34个测试)
```

## 🎯 测试覆盖范围

### 工具函数 (utils.test.ts)

- ✅ 时间格式化、JSON美化、字节格式化
- ✅ 规则验证、正则验证
- ✅ JSON diff算法、行级比较

### API客户端 (api.test.ts)

- ✅ Axios实例、错误处理、重试逻辑
- ✅ SSE连接管理、重连逻辑、事件解析
- ✅ 规则CRUD操作
- ✅ 请求列表、详情、清理
- ✅ 配置读写

### Hooks (hooks.test.ts)

- ✅ useRules - React Query hooks
- ✅ useRequests - 请求列表过滤
- ✅ useSSE - SSE连接管理
- ✅ useConfig - 配置读写

### Store (store.test.ts)

- ✅ app-store - 全局应用状态
- ✅ ui-store - UI状态管理

## 🔧 常用命令

```bash
# 运行所有测试
npm run test:run

# 监视模式（开发时使用）
npm run test:watch

# 生成覆盖率报告
npm run test:coverage

# 运行单个文件
npx vitest run tests/unit/utils.test.ts

# 验证测试结构
node tests/validate-tests.mjs
```

## 📊 测试统计

| 模块          | 套件    | 用例    | 状态 |
| ------------- | ------- | ------- | ---- |
| utils.test.ts | 21      | 69      | ✅   |
| api.test.ts   | 27      | 35      | ✅   |
| hooks.test.ts | 27      | 29      | ✅   |
| store.test.ts | 26      | 34      | ✅   |
| **总计**      | **101** | **167** | ✅   |

## 🎯 下一步

1. **运行测试验证**: `npm run test:run`
2. **检查覆盖率**: `npm run test:coverage`
3. **查看详细文档**: `frontend/tests/README.md`
4. **查看测试总结**: `frontend/tests/TEST_SUMMARY.md`

## 💡 提示

- 所有测试都是独立的，可以单独运行
- 使用 `npm run test:watch` 进行开发时实时测试
- 测试使用了完整的模拟，不需要真实后端
- 支持 TypeScript 类型检查

---

**测试框架已就绪，可以开始测试了！** 🎉
