# Design: Reusable Request Viewer Component

## Context

请求详情查看是 PromptXY 的核心功能之一。用户需要：
1. 查看原始请求和改写后请求的差异
2. 阅读 system prompt 等 Markdown 格式的大段文本
3. 定位到改写规则影响的特定字段
4. 理解复杂的嵌套请求结构

当前实现的痛点：
- 与 Claude API 格式强耦合
- 纯文本展示导致 Markdown 格式丢失
- 没有折叠/展开控制，信息密度过高
- DIFF 视图对复杂结构支持不佳

## Goals / Non-Goals

### Goals

- 提供可复用的请求详情查看组件，支持多种 API 格式
- 完整的 Markdown 渲染支持（标题、列表、代码块、表格、数学公式等）
- 智能的折叠/展开控制，降低信息密度
- 清晰的差异对比视图，支持段落级对比
- 配置驱动的字段渲染行为

### Non-Goals

- 不实现请求编辑功能（只读视图）
- 不实现请求历史对比（仅对比原始 vs 改写后）
- 不实现复杂的性能优化（虚拟滚动等），除非实际数据量证明需要

## Decisions

### Decision 1: 适配器模式架构

**选择**: 使用适配器模式解耦核心组件与具体 API 格式

**原因**:
- PromptXY 未来可能支持 OpenAI、Gemini 等多种 API
- 每种 API 的请求结构、关键字段、改写关注点都不同
- 适配器允许核心组件保持通用性

**实现**:
```typescript
interface RequestAdapter<T = any> {
  readonly name: string;
  readonly version: string;
  supports(request: any): request is T;
  extractMetadata(request: T): RequestMetadata;
  buildViewTree(request: T, original?: T): ViewNode;
  getFieldConfig(path: string): FieldConfig | undefined;
  getGroups?(viewTree: ViewNode): ViewGroup[];
}
```

**替代方案**:
- 配置文件驱动: 灵活性较低，难以处理复杂逻辑
- 单体组件支持多种格式: 代码耦合严重，难以扩展

### Decision 2: 三视图模式

**选择**: 提供结构概览、内容详情、差异对比三种视图模式

**原因**:
- **结构概览**: 默认视图，快速定位关注点（system prompt、tools）
- **内容详情**: 深入阅读具体内容，支持折叠/展开和搜索
- **差异对比**: 专注查看改写效果，仅显示变化部分

**默认视图**: 结构概览（因为 system prompt 是主要关注点）

### Decision 3: Markdown 渲染方案

**选择**: `react-markdown` + `remark-gfm` + `rehype-highlight` + `remark-math` + `rehype-katex`

**原因**:
- `react-markdown`: 成熟的 React Markdown 渲染库
- `remark-gfm`: 支持 GitHub Flavored Markdown（表格、删除线等）
- `rehype-highlight`: 代码高亮，支持 180+ 语言
- `remark-math` + `rehype-katex`: LaTeX 数学公式渲染（行内 `$...$` 和块级 `$$...$$`）
- 生态成熟，维护活跃

**数学公式支持**:
- 行内公式: `$E = mc^2$` → E = mc²
- 块级公式: `$$\sum_{i=1}^{n} x_i$$` → Σxi
- system prompt 中常见数学符号、算法描述

**安全考虑**:
- 使用 `rehype-sanitize` 防范 XSS
- 不允许 HTML 标签直接渲染

**替代方案**:
- `marked` + 自定义渲染: 需要更多安全工作
- 自己实现解析: 过于复杂，不推荐

### Decision 4: 节点类型系统

**选择**: 定义 6 种节点类型，每种类型有专门的渲染器

| 类型 | 描述 | 渲染器 |
|------|------|--------|
| `PRIMITIVE` | 简单值（string, number, boolean, <200字符） | 纯文本 |
| `STRING_LONG` | 长字符串（>200字符） | 折叠 + 复制 |
| `MARKDOWN` | Markdown 格式文本 | Markdown 渲染器 |
| `JSON` | JSON 对象 | 树形展示 |
| `ARRAY` | 数组 | 列表展示 |
| `CODE` | 代码块 | 代码高亮 |

**原因**:
- 不同类型的内容有不同的展示需求
- 允许自定义渲染器覆盖默认行为
- 便于扩展新的内容类型

### Decision 5: 智能折叠规则与状态持久化

**选择**: 基于字段配置和内容特征自动判断折叠状态，用户折叠状态存储在 localStorage

**默认规则**:
- System Prompt: 默认展开（因为是关注重点）
- Messages 数组: 默认展开前 3 条
- Tools 数组: 默认折叠
- 字符串 > 500 字符: 可折叠
- 数组 > 10 项: 可折叠
- 嵌套深度 > 4: 可折叠

**可配置性**:
- 通过 `FieldConfig` 为每个字段指定折叠行为
- 用户手动展开/折叠状态存储在 localStorage

**localStorage 存储方案**:
- Key: `request-viewer:collapse-state`
- Value: JSON 对象，格式 `{ [nodeId]: boolean }`
- 示例: `{ "messages.0.content": true, "tools": false }`
- 无需服务端存储，简单高效

### Decision 6: 差异对比策略

**选择**: 段落级对比（对于 Markdown）+ 行级对比（对于 JSON/代码）

**原因**:
- 纯文本行级对比对 Markdown 不友好（段落移动 = 大量红色）
- 段落级对比能更好地识别结构性变化
- JSON/代码保持行级对比

**实现**:
- 将 Markdown 解析为语法树（AST）
- 对比段落级别的节点
- 相同段落自动折叠

## Risks / Trade-offs

### Risk 1: Markdown 渲染性能

**风险**: 大段 Markdown（如 8000 字符的 system prompt）渲染可能较慢

**缓解措施**:
- 使用虚拟滚动（如果性能问题严重）
- 提供摘要模式（仅渲染前 N 段）
- 延迟渲染（用户点击展开时才渲染）

### Risk 2: XSS 安全风险

**风险**: 用户输入的 Markdown 可能包含恶意脚本

**缓解措施**:
- 使用 `rehype-sanitize` 清理 HTML
- 考虑使用 DOMPurify
- 禁用危险的 HTML 标签（`<script>`, `<iframe>` 等）

### Risk 3: 适配器数量增长

**风险**: 每种新的 API 格式都需要编写适配器

**缓解措施**:
- 提供清晰的适配器文档和示例
- 考虑未来提供配置文件驱动的适配器生成器

### Trade-off 1: 复杂度 vs 灵活性

**选择**: 优先考虑灵活性（适配器模式、配置驱动）

**代价**: 初始实现较复杂，学习曲线较陡

**理由**: PromptXY 需要支持多种 API 格式，灵活性是长期价值

### Trade-off 2: 功能范围

**选择**: 聚焦于查看功能，不实现编辑

**代价**: 用户无法直接在详情页修改请求

**理由**: 编辑功能属于不同 scope，应单独提案

## Migration Plan

### Phase 1: 核心组件开发
1. 实现 `ViewNode` 类型系统和树构建器
2. 实现基础渲染器（Primitive, StringLong, Json, Array）
3. 实现 `RequestDetailPanel` 主组件
4. 实现 Claude API 适配器

### Phase 2: Markdown 渲染
1. 实现 MarkdownRenderer 组件
2. 集成代码高亮和数学公式渲染
3. 添加目录导航
4. 添加全屏阅读模式

### Phase 3: 差异对比
1. 实现段落级 Markdown diff
2. 实现"仅显示变化"功能
3. 添加差异导航（下一个/上一个差异）

### Phase 4: 集成与替换
1. 删除旧的 `RequestDetail.tsx` 和 `DiffViewer.tsx`
2. 在 `RequestsPage.tsx` 中使用新的 `RequestDetailPanel`
3. 测试和调试

## Open Questions

1. **是否需要虚拟滚动？**
   - 取决于实际性能测试
   - 如果 10000+ 行的请求体渲染流畅，则不需要

2. **是否支持导出请求体为文件？**
   - 可能是独立 feature
   - 建议单独提案

## Component Structure

```
frontend/src/components/request-viewer/
├── index.ts                        # 导出入口
├── types/
│   ├── index.ts                    # 核心类型定义
│   └── adapter.ts                  # 适配器接口
├── adapters/
│   ├── Registry.ts                 # 适配器注册表
│   ├── utils/
│   │   └── treeBuilder.ts          # 树构建工具
│   └── claude/
│       └── ClaudeMessagesAdapter.ts
├── components/
│   ├── RequestDetailPanel.tsx      # 主面板组件
│   ├── MetadataBar.tsx             # 元数据栏
│   ├── renderers/
│   │   ├── NodeRenderer.tsx        # 节点渲染器入口
│   │   ├── PrimitiveRenderer.tsx
│   │   ├── StringLongRenderer.tsx
│   │   ├── MarkdownRenderer.tsx
│   │   ├── JsonRenderer.tsx
│   │   ├── ArrayRenderer.tsx
│   │   └── CodeRenderer.tsx
│   └── views/
│       ├── SummaryView.tsx         # 概要视图
│       ├── FullView.tsx            # 完整视图
│       └── DiffView.tsx            # 差异视图
└── utils/
    ├── diff.ts                     # diff 算法
    └── markdown.ts                 # Markdown 工具
```

## UI Design Preview

### 整体布局

```
┌─────────────────────────────────────────────────────────────────────┐
│  ← 返回列表              请求详情                           [×] 关闭 │
├─────────────────────────────────────────────────────────────────────┤
│  ID: req_abc123    时间: 2025-12-25 10:30    客户端: claude-code   │
│  Model: mimo-v2-flash    状态: 200    耗时: 1.2s                   │
├─────────────────────────────────────────────────────────────────────┤
│  [ 结构概览 ] [ 内容详情 ] [ 差异对比 ]          🔍 搜索字段       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│                        视图内容区                                    │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 视图 1: 结构概览 (SummaryView) - 默认视图

```
┌─────────────────────────────────────────────────────────────────────┐
│  [ 结构概览 ] [ 内容详情 ] [ 差异对比 ]          🔍 搜索字段       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ 📋 基本信息                                    [展开 ▼]      │   │
│  ├─────────────────────────────────────────────────────────────┤   │
│  │ Model: mimo-v2-flash                                         │   │
│  │ Max Tokens: 4096                                             │   │
│  │ Messages: 1 条                                               │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ 📝 System Prompt (2 个片段, ~8,200 字符)       [展开 ▼]      │   │
│  ├─────────────────────────────────────────────────────────────┤   │
│  │ ┌─────────────────────────────────────────────────────────┐ │   │
│  │ │ [1] "You are Claude Code, Anthropic's official CLI..." │ │   │
│  │ │                                                           │ │   │
│  │ │ # Tone and style                                        │ │   │
│  │ │                                                           │ │   │
│  │ │ - Only use emojis if the user explicitly requests...    │ │   │
│  │ │ - Your output will be displayed on a command line...    │ │   │
│  │ │                                                           │ │   │
│  │ │             [继续阅读 →] [全屏阅读 ↗]                    │ │   │
│  │ └─────────────────────────────────────────────────────────┘ │   │
│  │ ┌─────────────────────────────────────────────────────────┐ │   │
│  │ │ [2] "You are an interactive CLI tool that helps..."    │ │   │
│  │ │     [展开内容 →]                                         │ │   │
│  │ └─────────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ 💬 Messages (1 条)                             [展开 ▼]      │   │
│  ├─────────────────────────────────────────────────────────────┤   │
│  │ ▶ messages[0].role: user                                    │   │
│  │ ▶ messages[0].content: [3 个文本片段]                       │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ 🔧 Tools (68 个)                                [展开 ▼]      │   │
│  ├─────────────────────────────────────────────────────────────┤   │
│  │ Task, TaskOutput, Bash, Glob, Grep, Read, Edit, Write...    │   │
│  │                                    [查看完整列表 →]         │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                      │
│  [展开全部内容]                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 视图 2: 内容详情 (FullView)

```
┌─────────────────────────────────────────────────────────────────────┐
│  [ 结构概览 ] [ 内容详情 ] [ 差异对比 ]          🔍 搜索字段       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ▼ model                                                            │
│    "mimo-v2-flash"                                                  │
│                                                                      │
│  ▼ messages                                                         │
│    ▼ [0]                                                            │
│      ▶ role: "user"                                                │
│      ▼ content (3 items)                                           │
│        ▼ [0]                                                        │
│          ▶ type: "text"                                            │
│          ▼ text (240 字符, 已折叠) ▶                               │
│            <system-reminder>This is a reminder...                  │
│            ...[展开内容 →]                                         │
│                                                                      │
│        ▼ [1]                                                        │
│          ▶ type: "text"                                            │
│          ▼ text (3200 字符, 已折叠) ▶                              │
│            Contents of /home/ekko.bao/work/promptxy/CLAUDE.md...    │
│            ...[展开内容 →]                                         │
│                                                                      │
│        ▼ [2]                                                        │
│          ▶ type: "text"                                            │
│          ▼ text: "你好"                                             │
│          cache_control: { type: "ephemeral" }                       │
│                                                                      │
│    ▼ system (2 items, 已折叠) ▶                                     │
│    ▼ tools (68 items, 已折叠) ▶ [展开前 10 个]                     │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 视图 3: 差异对比 (DiffView)

```
┌─────────────────────────────────────────────────────────────────────┐
│  [ 结构概览 ] [ 内容详情 ] [ 差异对比 ]                              │
├─────────────────────────────────────────────────────────────────────┤
│  [☑ 仅显示变化]  [☑ 忽略无意义空白]  [ 显示行号 ]                   │
├─────────────────────────────────────────────────────────────────────┤
│  原始请求                            │  修改后请求                    │
├─────────────────────────────────────┼───────────────────────────────┤
│                                     │                               │
│  🟢 未变化 (已折叠 1200 行)         │  🟢 未变化 (已折叠 1200 行)   │
│                                     │                               │
├─────────────────────────────────────┼───────────────────────────────┤
│                                     │                               │
│  ▼ messages                         │  ▼ messages                   │
│    ▼ [0] content[2].text           │    ▼ [0] content[2].text     │
│      "你好"                         │      "你好，请帮我写代码" 🔴  │
│                                     │                               │
├─────────────────────────────────────┼───────────────────────────────┤
│                                     │                               │
│  ▼ system                           │  ▼ system                     │
│    ▼ [0] text                       │    ▼ [0] text                 │
│      # Tone and style               │      # Tone and style         │
│                                     │                               │
│      🟢 [段落 2-5, 无变化]         │      🟢 [段落 2-5, 无变化]    │
│                                     │                               │
│      ## Planning without timelines  │      ## Planning without...   │
│      Focus on what needs...        │      Focus on what needs...   │
│                                     │                               │
│      🟢 [其余 150 行, 无变化]       │      🟢 [其余 150 行, 无变化] │
│                                     │                               │
│  ▼ tools (68 项, 无变化)            │  ▼ tools (68 项, 无变化)      │
│                                     │                               │
├─────────────────────────────────────────────────────────────────────┤
│  [ 上一个差异 ↑ ]  [ 下一个差异 ↓ ]                                 │
└─────────────────────────────────────────────────────────────────────┘

🟢 = 无变化 | 🔴 = 修改 | 🟢 = 新增 | 🔴 = 删除
```

### Markdown 全屏阅读模式

```
┌─────────────────────────────────────────────────────────────────────────┐
│  System Prompt - system[0].text                        [×] 全屏模式     │
├─────────────────────────────────────────────────────────────────────────┤
│  [复制 Markdown] [复制纯文本]                         TOC: >           │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  You are Claude Code, Anthropic's official CLI for Claude.             │
│                                                                          │
│  You are an interactive CLI tool that helps users with                 │
│  software engineering tasks...                                         │
│                                                                          │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                                          │
│  # Tone and style                                                       │
│                                                                          │
│  - Only use emojis if the user explicitly requests it                  │
│  - Your output will be displayed on a command line interface...        │
│                                                                          │
│  # Professional objectivity                                            │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────┐    │
│  │ > Prioritize technical accuracy and truthfulness over...       │    │
│  │ > Focus on facts and problem-solving...                       │    │
│  └────────────────────────────────────────────────────────────────┘    │
│                                                                          │
│  ## Planning without timelines                                           │
│  Focus on what needs to be done, not when...                           │
│                                                                          │
│  ### Task Management                                                     │
│  The formula for efficiency is:                                          │
│                                                                          │
│  $$E = \frac{Work}{Time}$$                                              │
│                                                                          │
│  And for large datasets:                                                │
│                                                                          │
│  $$\sum_{i=1}^{n} x_i \leq Threshold$$                                  │
│                                                                          │
│  ```typescript                                                          │
│  function calculateEfficiency(work: number, time: number): number {    │
│    return work / time;                                                  │
│  }                                                                       │
│  ```                                                                     │
│                                                                          │
│  | Feature | Priority | Status |                                       │
│  |---------|----------|--------|                                       │
│  | 三视图模式 | P0 | 待实现 |                                          │
│  | Markdown 渲染 | P0 | 待实现 |                                        │
│  | 数学公式 | P0 | 待实现 |                                            │
│                                                                          │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│        目录                                      ▲                       │
│  • Tone and style                            │                       │
│  • Professional objectivity                  │ 滚动                   │
│    • Planning without timelines              ▼                       │
│    • Task Management                                                  │
└─────────────────────────────────────────────────────────────────────────┘
      ▲                                                    ▲
      │                                                    │
   右侧固定目录                                       点击跳转
```

### 组件样式规范

#### 色彩系统

| 用途 | 颜色 | Tailwind Class |
|-----|------|----------------|
| 无变化 | 🟢 灰色 | `text-gray-500`, `bg-gray-50` |
| 新增 | 🟢 绿色 | `text-green-600`, `bg-green-50` |
| 删除 | 🔴 红色 | `text-red-600`, `bg-red-50` |
| 修改 | 🟡 黄色 | `text-yellow-600`, `bg-yellow-50` |
| 折叠指示器 | 灰色 | `text-gray-400` |
| 边框 | 灰色 | `border-gray-200 dark:border-gray-700` |
| 背景（亮色） | 白色 | `bg-white` |
| 背景（暗色） | 深灰 | `bg-gray-900` |

#### 间距规范

| 元素 | 间距 |
|-----|------|
| 卡片内边距 | `p-4` |
| 节点缩进 | 每层 `ml-4` |
| 节点间距 | `my-2` |
| 按钮间距 | `gap-2` |
| 分隔线间距 | `my-4` |

#### 字体规范

| 元素 | 字号 | 字重 |
|-----|------|------|
| 标题 H1 | `text-2xl` | `font-bold` |
| 标题 H2 | `text-xl` | `font-bold` |
| 标题 H3 | `text-lg` | `font-bold` |
| 正文 | `text-sm` | `font-normal` |
| 代码/JSON | `text-xs` | `font-mono` |

#### 交互状态

| 状态 | 样式 |
|-----|------|
| Hover | `hover:bg-gray-100 dark:hover:bg-gray-800` |
| Active | `bg-gray-200 dark:bg-gray-700` |
| Focus | `ring-2 ring-primary` |
| Disabled | `opacity-50 cursor-not-allowed` |
