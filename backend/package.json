{
  "name": "promptxy-backend",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "PromptXY v2.0 Backend - HTTP Proxy Gateway + API Server",
  "scripts": {
    "dev": "tsx --watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "test": "tsx --test tests/*.test.ts"
  },
  "dependencies": {
    "sqlite3": "^5.1.7",
    "sqlite": "^4.2.1"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.2"
  }
}
