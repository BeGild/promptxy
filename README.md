# PromptXY v2.0

> 本地HTTP代理规则管理器 - 完整的前后端分离架构

## ✨ 核心功能

### 🎯 请求拦截与修改

- **实时捕获**: 记录所有经过代理的请求
- **规则引擎**: 7种操作类型，支持正则表达式
- **多客户端支持**: Claude Code, Codex CLI, Gemini CLI
- **差异对比**: 可视化原始 vs 修改后请求

### 📊 请求历史

- **SQLite存储**: 高效的本地数据库
- **自动清理**: 保留最近100条请求（可配置）
- **实时推送**: SSE实时更新UI
- **搜索筛选**: 按客户端、时间、内容筛选

### 🎨 Web UI

- **规则管理**: 可视化CRUD操作
- **请求监控**: 实时查看请求流
- **预览测试**: 沙盒环境测试规则
- **配置管理**: 导入/导出配置

## 🏗️ 架构概览

```
┌─────────────────────────────────────────────────────────────┐
│                         用户操作                             │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ↓
┌─────────────────────────────────────────────────────────────┐
│                    前端 (React + HeroUI)                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │  规则管理    │  │  请求查看    │  │  预览测试    │     │
│  │  (UI层)      │  │  (UI层)      │  │  (UI层)      │     │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘     │
│         │                 │                 │              │
│         └────────┬────────┴────────┬────────┘              │
│                  │                 │                       │
│         ┌────────▼────────┐  ┌─────▼──────┐               │
│         │  API客户端      │  │  SSE客户端 │               │
│         │  (fetch/axios)  │  │  (EventSource)│             │
│         └────────┬────────┘  └─────┬──────┘               │

                   │                  │
                   ↓                  ↓
┌─────────────────────────────────────────────────────────────┐
│                    后端 (Node.js + TS)                       │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │  API服务器   │  │  SSE推送     │  │  代理核心    │     │
│  │  (端口7071)  │  │  (端口7071)  │  │  (端口7070)  │     │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘     │
│         │                 │                 │              │
│         └────────┬────────┴────────┬────────┘              │
│                  │                 │                       │
│         ┌────────▼────────┐  ┌─────▼──────┐               │
│         │  SQLite数据库   │  │  配置文件  │               │
│         │  (请求历史)     │  │  (规则)    │               │
│         └─────────────────┘  └────────────┘               │
└─────────────────────────────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────────┐
│                    CLI工具 (Claude/Codex/Gemini)             │
│  发起请求 → 经过代理(7070) → 规则修改 → 转发上游API          │
└─────────────────────────────────────────────────────────────┘
```

## 🚀 快速开始

### 前置要求

- Node.js 18+
- npm 或 pnpm

### 开发环境

**1. 克隆并安装依赖**

```bash
chmod +x scripts/*.sh
```

**2. 启动开发服务**

```bash
./scripts/dev.sh
```

这将同时启动：

- 后端 Gateway (端口 7070)
- 后端 API Server (端口 7071)
- 前端开发服务器 (端口 5173)

**3. 访问 Web UI**

```
http://localhost:5173
```

### 生产部署

**1. 构建项目**

```bash
./scripts/build.sh
```

**2. 启动服务**

```bash
cd backend
npm start
```

访问: `http://127.0.0.1:7071`

## 🔧 CLI 配置

### Claude Code

```bash
export ANTHROPIC_BASE_URL="http://127.0.0.1:7070/claude"
```

### Codex CLI

```bash
export OPENAI_BASE_URL="http://127.0.0.1:7070/openai"
```

### Gemini CLI

```bash
export GOOGLE_GEMINI_BASE_URL="http://127.0.0.1:7070/gemini"
```

> **注意**：v2.0 开始，所有 CLI 配置都必须带上路径前缀（`/claude`、`/openai`、`/gemini`）。

## 📝 配置文件

配置文件位置: `~/.promptxy/config.json` 或项目根目录 `promptxy.config.json`

```json
{
  "listen": {
    "host": "127.0.0.1",
    "port": 7070
  },
  "api": {
    "host": "127.0.0.1",
    "port": 7071
  },
  "suppliers": [
    {
      "id": "claude-anthropic",
      "name": "Claude (Anthropic)",
      "baseUrl": "https://api.anthropic.com",
      "localPrefix": "/claude",
      "pathMappings": [],
      "enabled": true
    },
    {
      "id": "openai-official",
      "name": "OpenAI Official",
      "baseUrl": "https://api.openai.com",
      "localPrefix": "/openai",
      "pathMappings": [],
      "enabled": true
    },
    {
      "id": "gemini-google",
      "name": "Gemini (Google)",
      "baseUrl": "https://generativelanguage.googleapis.com",
      "localPrefix": "/gemini",
      "pathMappings": [],
      "enabled": true
    }
  ],
  "rules": [
    {
      "id": "rule-001",
      "description": "添加自定义系统提示",
      "when": {
        "client": "claude",
        "field": "system"
      },
      "ops": [
        {
          "type": "append",
          "text": "\n\n## Custom Rules\n- Be concise\n- Use markdown"
        }
      ],
      "enabled": true
    }
  ],
  "storage": {
    "maxHistory": 100,
    "autoCleanup": true,
    "cleanupInterval": 1
  },
  "debug": false
}
```

## 🎨 规则类型

### 支持的操作类型

| 类型            | 描述         | 参数                           |
| --------------- | ------------ | ------------------------------ |
| `set`           | 完全替换     | `text`                         |
| `append`        | 追加到末尾   | `text`                         |
| `prepend`       | 插入到开头   | `text`                         |
| `replace`       | 替换匹配内容 | `match`/`regex`, `replacement` |
| `delete`        | 删除匹配内容 | `match`/`regex`                |
| `insert_before` | 在匹配前插入 | `regex`, `text`                |
| `insert_after`  | 在匹配后插入 | `regex`, `text`                |

### 匹配条件

- `client`: `claude` | `codex` | `gemini`
- `field`: `system` | `instructions`
- `method`: HTTP方法 (可选)
- `pathRegex`: 路径正则 (可选)
- `modelRegex`: 模型正则 (可选)

## 📚 API 端点

### Gateway (端口 7070)

- `GET /_promptxy/health` - 健康检查
- 其他请求 → 转发到上游API

### API Server (端口 7071)

- `GET /_promptxy/events` - SSE实时推送
- `GET /_promptxy/requests` - 请求历史列表
- `GET /_promptxy/requests/:id` - 请求详情
- `DELETE /_promptxy/requests/:id` - 删除请求
- `GET /_promptxy/config` - 获取配置
- `POST /_promptxy/config/sync` - 同步配置
- `POST /_promptxy/preview` - 预览规则
- `POST /_promptxy/requests/cleanup` - 清理数据
- `GET /_promptxy/stats` - 统计信息
- `GET /_promptxy/database` - 数据库信息

## 🛠️ 开发

### 项目结构

```
promptxy/
├── backend/                    # 后端服务
│   ├── src/
│   │   ├── main.ts            # 应用入口
│   │   ├── promptxy/
│   │   │   ├── gateway.ts     # 代理核心
│   │   │   ├── api-server.ts  # API服务器
│   │   │   ├── database.ts    # SQLite操作
│   │   │   ├── config.ts      # 配置管理
│   │   │   ├── types.ts       # 类型定义
│   │   │   ├── rules/         # 规则引擎
│   │   │   └── adapters/      # 适配器
│   │   └── ...
│   ├── package.json
│   └── tsconfig.json
│
├── frontend/                   # 前端应用
│   ├── src/
│   │   ├── main.tsx           # 应用入口
│   │   ├── App.tsx            # 主组件
│   │   ├── components/        # UI组件
│   │   ├── pages/             # 页面
│   │   ├── hooks/             # React Hooks
│   │   ├── api/               # API客户端
│   │   ├── store/             # 状态管理
│   │   ├── types/             # 类型定义
│   │   ├── utils/             # 工具函数
│   │   └── styles/            # 样式
│   ├── package.json
│   ├── vite.config.ts
│   └── tsconfig.json
│
├── scripts/                    # 脚本
│   ├── dev.sh                 # 开发环境
│   ├── build.sh               # 构建
│   └── start.sh               # 生产启动
│
├── docs/                       # 文档
└── README.md
```

### 技术栈

**后端**

- Node.js + TypeScript
- SQLite (数据库)
- Express-like (HTTP服务器)

**前端**

- React 18
- HeroUI (组件库)
- Zustand (状态管理)
- TanStack Query (数据获取)
- Vite (构建工具)
- Tailwind CSS

## 🔒 安全性

- **本地运行**: 默认绑定 127.0.0.1
- **敏感信息**: 不存储API密钥
- **数据清理**: 自动清理旧请求
- **CORS**: 开发环境启用，生产环境禁用

## 📊 性能

- **数据库**: SQLite处理1000+请求无压力
- **前端**: 虚拟滚动支持长列表
- **实时**: SSE轻量级推送
- **内存**: 自动清理限制在100条记录

## 🤝 兼容性

- **向后兼容**: V2.0是V1的超集
- **无破坏性**: 现有配置无需修改
- **渐进式**: 可选功能，不影响CLI使用

## 📄 许可

MIT License

---

**PromptXY v2.0** - 让规则管理变得简单而强大
